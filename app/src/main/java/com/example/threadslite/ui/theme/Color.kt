package com.example.threadslite.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary palette: Black & White ───────────────────────────────────────────
val Black        = Color(0xFF0A0A0A)   // Near-black for primary text / buttons
val White        = Color(0xFFFFFFFF)
val OffWhite     = Color(0xFFFAFAFA)   // App background

// ─── Grey scale ───────────────────────────────────────────────────────────────
val Grey100      = Color(0xFFF5F5F5)   // Card surfaces
val Grey200      = Color(0xFFEEEEEE)   // Dividers, borders
val Grey400      = Color(0xFFBDBDBD)   // Subtle borders
val Grey600      = Color(0xFF757575)   // Secondary text
val Grey800      = Color(0xFF424242)   // Caption / timestamp text

// ─── Semantic colours (kept neutral, no vivid brand colours) ─────────────────
val ErrorRed     = Color(0xFFD32F2F)   // Error messages only
val LinkBlue     = Color(0xFF1565C0)   // Tappable location links
